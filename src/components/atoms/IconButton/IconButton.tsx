import React from 'react';

export interface IconButtonProps {
  // TODO: Define props for Button with only icon, no text
}

const IconButton: React.FC<IconButtonProps> = ({
  // TODO: Implement Button with only icon, no text
}) => {
  return (
    <div>
      {/* TODO: Implement IconButton component */}
      <p>IconButton component - To be implemented</p>
      <p className="text-sm text-gray-500">Button with only icon, no text</p>
    </div>
  );
};

export default IconButton;
